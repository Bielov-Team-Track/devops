[Unit]
# Container port publishes bind ${WG_BIND_IP} (a wg0 address). If Docker starts
# before wg0 exists, every wg-bound publish fails at container start.
After=wg-quick@wg0.service
Wants=wg-quick@wg0.service
