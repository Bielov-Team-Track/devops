# Sorts before 50-cloud-init.conf so these win (sshd: first value wins).
# cloud-init rewrites 50-cloud-init.conf with PasswordAuthentication yes on reboot.
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin prohibit-password
